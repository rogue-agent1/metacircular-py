# metacircular-py 🏆
Metacircular evaluator — a Lisp that can interpret itself via `eval`. Tool #300.
Features: closures, let, cond, higher-order functions, quasiquote, and self-interpretation.
Zero dependencies.
```
python3 metacircular-py.py --test
```
